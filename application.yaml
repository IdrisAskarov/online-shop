eureka:
  instance:
    prefer-ip-address: true
  client: 
    fetch-registry: true
    register-with-eureka:  true
    #service-url:
      #domain-zone: http://${EUREKA_SERVER_ADDRESS:locahost:8766/eureka1}
